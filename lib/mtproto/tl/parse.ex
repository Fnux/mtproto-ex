defmodule MTProto.TL.Parse do
  #def parse(data) do
  #    << method :: little-size(4)-unit(8) >> = :binary.part(data, 0, 4)
  #  values = :binary.part data, 4, byte_size(data) - 4
  #  {method, values}
  #end
end
